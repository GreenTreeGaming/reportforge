import{money}from"@/lib/reportforge/professional-report/formatters";import type{ReportModel}from"@/lib/reportforge/professional-report/types";import{ReportPage}from"./ProfessionalReport";export function ExecutiveSummaryPage({model:m,page}:{model:ReportModel;page:number}){const risks=m.quality.limitations.slice(0,3);return <ReportPage number={page} title="Executive summary"><div className="heading"><p className="eyebrow">Executive summary</p><h1>{m.headline}</h1><p>{m.narrative}</p></div>{m.metadata.executiveNote&&<aside><b>Management note</b><p>{m.metadata.executiveNote}</p></aside>}<div className="kpis">{m.kpis.map(k=><article key={k.id}><span>{k.label}</span><strong>{k.value}</strong><b className={k.direction}>{k.change??"No comparison"}</b><small>{k.context}</small></article>)}</div><div className="columns"><section className="card"><h2>Positive signals</h2>{m.drivers.filter(x=>x.impact>0).slice(0,3).map(x=><div className="insight good" key={x.label}><b>{x.label}</b><strong>{money(x.impact)}</strong><p>{x.description}</p></div>)}</section><section className="card"><h2>Risks to address</h2>{risks.map(x=><div className="insight risk" key={x}><b>Data or performance limitation</b><p>{x}</p></div>)}</section></div><section className="dark"><p className="eyebrow">Immediate priorities</p><h2>Actions backed by the strongest available evidence</h2>{m.actions.slice(0,3).map((a,i)=><article key={a.id}><b>{String(i+1).padStart(2,"0")}</b><div><strong>{a.title}</strong><p>{a.recommendation}</p></div><span>{a.impact===null?"Not quantified":money(a.impact)}</span></article>)}</section></ReportPage>}
