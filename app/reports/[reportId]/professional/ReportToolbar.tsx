"use client";import Link from"next/link";import{useRef,useState}from"react";import type{ReportSettings}from"@/lib/reportforge/professional-report/types";export function ReportToolbar({reportId,settings,onChange,onSave,saving}:{reportId:string;settings:ReportSettings;onChange:(s:ReportSettings)=>void;onSave:()=>Promise<void>;saving:boolean}){const[open,setOpen]=useState(false),ref=useRef<HTMLInputElement>(null);async function logo(file?:File){if(!file)return;if(!file.type.startsWith("image/")||file.size>2_000_000){alert("Choose an image smaller than 2 MB.");return}const data=await new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>typeof r.result==="string"?resolve(r.result):reject();r.onerror=reject;r.readAsDataURL(file)});onChange({...settings,logoDataUrl:data})}return <><nav className="toolbar no-print"><span className="brand"><b>RF</b><i>Professional report</i></span><div><Link href={`/reports/${reportId}/overview`}>Back to overview</Link><button onClick={()=>setOpen(true)}>Customize</button><button className="primary" onClick={()=>window.print()}>Save as PDF</button></div></nav>{open&&<div className="modal no-print"><button className="backdrop" onClick={()=>setOpen(false)}/><aside><header><div><small>Report settings</small><h2>Customize the document</h2></div><button onClick={()=>setOpen(false)}>×</button></header><main><label>Business name<input value={settings.businessName} onChange={e=>onChange({...settings,businessName:e.target.value})}/></label><label>Report title<input value={settings.reportTitle} onChange={e=>onChange({...settings,reportTitle:e.target.value})}/></label><div className="form-grid"><label>Prepared for<input value={settings.preparedFor??""} onChange={e=>onChange({...settings,preparedFor:e.target.value||null})}/></label><label>Prepared by<input value={settings.preparedBy??""} onChange={e=>onChange({...settings,preparedBy:e.target.value||null})}/></label></div><label>Reporting label<input value={settings.reportingLabel} onChange={e=>onChange({...settings,reportingLabel:e.target.value})}/></label><label>Executive note<textarea rows={5} value={settings.executiveNote??""} onChange={e=>onChange({...settings,executiveNote:e.target.value||null})}/></label><div className="form-grid"><label>Accent color<input type="color" value={settings.accentColor} onChange={e=>onChange({...settings,accentColor:e.target.value})}/></label><label>Page size<select value={settings.pageSize} onChange={e=>onChange({...settings,pageSize:e.target.value==="letter"?"letter":"a4"})}><option value="a4">A4</option><option value="letter">US Letter</option></select></label></div><div className="logo-row"><input ref={ref} hidden type="file" accept="image/*" onChange={e=>void logo(e.target.files?.[0])}/><button onClick={()=>ref.current?.click()}>{settings.logoDataUrl?"Replace logo":"Upload logo"}</button>{settings.logoDataUrl&&<button onClick={()=>onChange({...settings,logoDataUrl:null})}>Remove</button>}</div><label className="check"><input type="checkbox" checked={settings.includeMethodology} onChange={e=>onChange({...settings,includeMethodology:e.target.checked})}/> Include methodology page</label></main><footer><button onClick={()=>setOpen(false)}>Cancel</button><button className="primary" disabled={saving} onClick={async()=>{await onSave();setOpen(false)}}>{saving?"Saving…":"Save changes"}</button></footer></aside></div>}</>}
