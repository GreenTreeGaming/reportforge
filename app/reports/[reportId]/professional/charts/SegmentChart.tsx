import{compactMoney}from"@/lib/reportforge/professional-report/formatters";import type{ReportModel}from"@/lib/reportforge/professional-report/types";export function SegmentChart({items}:{items:ReportModel["customer"]["segments"]}){const max=Math.max(1,...items.map(x=>x.customers));return <div className="segments">{items.map(x=><div key={x.segment}><header><strong>{x.label}</strong><span>{x.customers.toLocaleString()} customers · {compactMoney(x.revenue)}</span></header><i><b style={{width:`${x.customers/max*100}%`}}/></i></div>)}</div>}
